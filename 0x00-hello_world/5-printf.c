#include <stdio.h>

/*
 * main -serves as the entry point for the program
 * Return _Always 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
